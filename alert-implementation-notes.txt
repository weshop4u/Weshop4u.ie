Alert Sound Implementation Notes
=================================

STORE DASHBOARD (app/store-dashboard/index.tsx):
- Already has audio player using mixkit classic alarm URL (line 80)
- Already plays on new pending orders (line 174-178)
- Already has repeating alert every 30 seconds while pending orders exist (line 195-202)
- BUT: only plays on native (Platform.OS !== "web"), not web
- Need to: change repeat interval from 30s to shorter (e.g. 8-10s for urgency)
- Need to: add web audio support using HTML5 Audio API
- Need to: use local asset instead of remote URL for reliability

DRIVER DASHBOARD (app/driver/index.tsx):
- Has haptic vibration on new offer (line 145-149)
- Has push notification on new offer (line 151-165)
- NO audio alert currently
- Need to: add persistent looping alarm sound when offer appears
- Need to: stop alarm when offer is accepted/declined/expired
- Need to: add vibration pattern that repeats

DRIVER JOB OFFER (app/driver/job-offer.tsx):
- Standalone screen with mock data (not used in main flow)
- Main offer UI is inline in driver/index.tsx
- This screen is legacy/unused

SOUND FILE:
- Using: assets/sounds/order-alert.mp3 (classic alarm, 5 seconds)
- Also available via URL: https://assets.mixkit.co/active_storage/sfx/2869/2869-preview.mp3

PLAN:
1. Driver: Add useAudioPlayer with looping alarm when offer appears
2. Driver: Add repeating vibration pattern while offer is active
3. Store dashboard: Reduce repeat interval to 8 seconds
4. Store dashboard: Add web audio support
5. Both: Use the same sound file for consistency
